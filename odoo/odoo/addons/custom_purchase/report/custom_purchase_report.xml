<odoo>
    <template id="external_layout_purchase_custom">
        <div class="header o_company_#{company.id}_layout" t-att-style="report_header_style">
            <div class="row">
                <!-- Custom Header: Tagline left, Logo right -->
                <div class="col-9 text-center">
                    <div t-if="company.report_header" t-field="company.report_header" style="margin-top: 22px;" />
                </div>
                <div class="col-3 text-end">
                    <img t-if="company.logo" t-att-src="image_data_uri(company.logo)" style="max-height: 45px;" alt="Logo"/>
                </div>
            </div>
            <div class="row zero_min_height" t-if="company.logo or company.report_header">
                <div class="col-12">
                    <div style="border-bottom: 1px solid black;"/>
                </div>
            </div>
            <!-- Single-line Address -->
            <div class="row">
                <div class="col-12 text-center">
                    <t t-if="company.partner_id">
                        <t t-esc="', '.join(filter(None, [company.partner_id.name, company.partner_id.street, company.partner_id.city, company.partner_id.state_id.name if company.partner_id.state_id else '', company.partner_id.country_id.name if company.partner_id.country_id else '', company.partner_id.zip]))"/>
                    </t>
                </div>
            </div>
        </div>

        <t t-set="layout_background_url"
           t-value="'data:image/png;base64,%s' % company.layout_background_image.decode('utf-8') if company.layout_background_image and company.layout_background == 'Custom' else '/base/static/img/bg_background_template.jpg' if company.layout_background == 'Geometric' else ''"/>
        <div t-attf-class="article o_report_layout_standard o_company_#{company.id}_layout {{  'o_report_layout_background' if company.layout_background in ['Geometric', 'Custom']  else  '' }}"
             t-attf-style="{{ 'background-image: url(%s);' % layout_background_url if layout_background_url else '' }}"
             t-att-data-oe-model="o and o._name"
             t-att-data-oe-id="o and o.id"
             t-att-data-oe-lang="o and o.env.context.get('lang')">
            <div class="pt-5">
                <t t-call="web.address_layout"/>
            </div>
            <t t-out="0"/>
        </div>

        <!-- Footer: keep as default or customize -->
        <div class="footer o_standard_footer o_company_#{company.id}_layout">
            <div class="text-center" style="border-top: 1px solid black;">
                <ul class="list-inline mb4">
                    <div t-field="company.report_footer"/>
                </ul>
                <div t-if="report_type == 'pdf'" class="text-muted">
                    Page: <span class="page"/> / <span class="topage"/>
                </div>
                <div t-if="report_type == 'pdf' and display_name_in_footer" class="text-muted">
                    <span t-out="o.name"/>
                </div>
            </div>
        </div>
    </template>
</odoo>
